# Markdown files
*.md

Wait, that's not right - the markdown file was added, not built/generated. Since only source/config files were added without any build artifacts, dependencies, or temp files, the output should be empty.

(login_flowchart.md is a source file being tracked in version control)